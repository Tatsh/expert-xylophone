# CMakeLists.txt — Project/OpenGL
#
# This directory has no translation units of its own; it only descends into its
# subdirectories.


add_subdirectory(Layer)
add_subdirectory(Models)
add_subdirectory(Scene)
